    arch_map:
      x86_64: amd64
      aarch64: arm64
# docker rootless vars
    target_user: "{{ ansible_user_id }}"
    setuptool_path: /usr/bin/dockerd-rootless-setuptool.sh
    user_systemd_unit: "{{ user_home }}/.config/systemd/user/docker.service"

